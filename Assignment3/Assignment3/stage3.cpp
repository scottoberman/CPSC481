#include "stage3.h"

void Stage3(PathNode shortestPath[], const int SHORTEST_PATH_SIZE)
{
	// Traverse the nodes in the shortestPath array.
	// The shortestPath array should contain something
	// like (0, 1), (0,2), (1,2), (1,3), etc. The origin (0,0)
	// will not be in the array as the path always starts
	// at (0,0).
}