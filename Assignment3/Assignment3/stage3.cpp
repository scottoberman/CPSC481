#include "stage3.h"

void Stage3(std::queue<PathNode> shortestPath)
{
	// Traverse the nodes in the shortestPath queue.
	// The shortestPath queue should contain something
	// like (0, 1), (0,2), (1,2), (1,3), etc. The origin (0,0)
	// will not be in the queue as the path always starts
	// at (0,0).
}